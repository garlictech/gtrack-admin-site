import { Action } from '@ngrx/store';
import * as RouteInfoDataActions from '../../actions/route-info-data';
import { routeInfoDataReducer } from '../route-info-data';

describe('RouteInfoData reducers', () => {
  /*
  it('should handle initial state', () => {
    expect(
      hikeEditMapReducer(undefined, undefined)
    )
    .toEqual({mapId: ''});
  });
  */
});
