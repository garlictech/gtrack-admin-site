.content(*ngIf='!(uploadOptions?.uploading$ | async)')
  fa-icon([icon]="icon", size="2x", *ngIf='uploaded$ | async')
  p-fileUpload(
    name="myfile[]",
    [mode]="mode",
    customUpload="true",
    (uploadHandler)="uploadOptions.uploadHandler($event)",
    auto="true",
    [chooseLabel]="placeholder"
  )

ng-template(#spinner)
  garlictech-component-busy-indicator
