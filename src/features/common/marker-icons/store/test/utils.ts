import { State } from '../state';
/*
export const createFeatureState = ({ markers, icons } = {}): State => ({
  markers,
  icons
});

export const createState = (stateParams?): { 'features.marker-icons': State } => ({
  'features.marker-icons': createFeatureState(stateParams)
});
*/
